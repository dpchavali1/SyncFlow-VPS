# Firebase Configuration
# Get these values from Firebase Console → Project Settings → General → Your apps → Web app

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://your_project_id-default-rtdb.firebaseio.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Support Configuration (optional - defaults are set in next.config.js)
# NEXT_PUBLIC_SUPPORT_EMAIL=syncflow.contact@gmail.com
# NEXT_PUBLIC_SUPPORT_EMAIL_SUBJECT=[SyncFlow Web] Support Request

# Admin Configuration
# Username and password hash for admin login at /admin/login
# To generate a password hash, run this in browser console:
#   async function hash(pw) { const e = new TextEncoder(); const d = e.encode(pw); const h = await crypto.subtle.digest('SHA-256', d); return Array.from(new Uint8Array(h)).map(b => b.toString(16).padStart(2, '0')).join(''); }
#   hash('your_password').then(console.log)
NEXT_PUBLIC_ADMIN_USERNAME=admin
NEXT_PUBLIC_ADMIN_PASSWORD_HASH=your_sha256_password_hash_here
